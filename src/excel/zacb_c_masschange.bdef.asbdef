projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZACB_C_MassChange alias MassChange
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action generateTemplate;
  use action processFile;
}